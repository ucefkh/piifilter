services:
  piifilter:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - piifilter_cache:/tmp/piifilter
    restart: unless-stopped

volumes:
  piifilter_cache: